# Dependencies
node_modules/
.pnp
.pnp.js

# Build output — dist/ is intentionally committed so `npm install -g github:...`
# needs no build step (devDependencies aren't installed on a global git install).
# Rebuild with `npm run build` before committing source changes.
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage/
.nyc_output/

# Generated by kodemux itself in consumer repos (kept out of this source repo)
/.kodemux/

# vii-stack local artifacts
.vii/

# Personal planning docs — not for the public repo
/MARKETING-PLAN.md
