{
  "name": "codemux",
  "version": "0.0.1",
  "description": "Repo-native middleware & guardrail engine for AI coding tools — routes prompts to the optimal model/effort/mode and enforces git-level guardrails.",
  "type": "module",
  "license": "MIT",
  "author": "Vibhu Sharma",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vibhusharma101/codemux.git"
  },
  "keywords": [
    "ai",
    "claude",
    "cli",
    "middleware",
    "guardrails",
    "llm-routing"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo \"build wired up in PR #1\" && exit 0",
    "test": "echo \"tests wired up in PR #1\" && exit 0"
  }
}
